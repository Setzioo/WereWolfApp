<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:tools="http://schemas.android.com/tools"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        tools:context=".LoginActivity" android:id="@+id/loginLayout"
        android:background="@color/colorBackground">
    <Button
            android:text="@string/sign_in"
            android:layout_width="241dp"
            android:layout_height="wrap_content"
            android:id="@+id/signInButton"
            android:textSize="30sp"
            app:layout_constraintEnd_toEndOf="@+id/loginTitle" app:layout_constraintStart_toStartOf="@+id/loginTitle"
            android:layout_marginTop="105dp" app:layout_constraintTop_toBottomOf="@+id/loginTitle"
            android:background="@drawable/buttonshape"
    />
    <Button
            android:text="@string/sign_up"
            android:layout_width="239dp"
            android:layout_height="55dp"
            android:id="@+id/signButton"
            android:textSize="30sp" android:layout_marginTop="92dp"
            app:layout_constraintTop_toBottomOf="@+id/signInButton"
            app:layout_constraintEnd_toEndOf="@+id/signInButton"
            app:layout_constraintStart_toStartOf="@+id/signInButton"
            android:background="@drawable/buttonshape"
    />
    <ImageButton
            android:layout_width="80dp"
            android:layout_height="60dp" app:srcCompat="@android:drawable/ic_menu_revert"
            android:id="@+id/returnLoginButton"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintVertical_bias="0.0" app:layout_constraintHorizontal_bias="0.0"
            android:background="@color/colorBackground" android:scaleType="fitCenter"
            android:contentDescription="@string/bouton_retour"/>
    <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content" android:id="@+id/loginTitle"
            android:textStyle="bold" android:textColor="@color/colorBlanc" android:textSize="36sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" android:layout_marginTop="124dp"
            app:layout_constraintHorizontal_bias="0.5" android:text="@string/login"/>
</android.support.constraint.ConstraintLayout>